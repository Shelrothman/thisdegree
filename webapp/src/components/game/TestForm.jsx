/**
 * use this file to create the form for the game
 * only need to worry about the logic to render different parts of the form
 * so that i can debug whats happening in my PlayBoard and then bring in the actual data.
 */

import { useState, useEffect } from 'react';



function TestForm() {
    // const [gameStarted, setGameStarted] = useState(false);
    // const [movieList, setMovieList] = useState([]);
    // const [readyToBridge, setReadyToBridge] = useState(false);
    const [formState, setFormState] = useState({
        
    });
}

export default TestForm;