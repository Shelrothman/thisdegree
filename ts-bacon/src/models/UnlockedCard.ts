


export class UnlockedCard {
    movie: string;
    // movieAnswer?: string;
    actor: string;

    constructor(movie: string, actor: string) {
        this.movie = movie;
        this.actor = actor;
    }
}